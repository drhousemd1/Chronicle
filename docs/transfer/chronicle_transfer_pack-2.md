# Chronicle Transfer Pack 2 (Archived)

This file was previously a generated handoff artifact and has been intentionally reduced in-repo to keep repository footprint stable.

Use the App Dashboard export flows and Quality Hub Change Log for current operational handoff data.

If a full transfer snapshot is needed, regenerate it from the current codebase and store it as a release artifact instead of source control.
