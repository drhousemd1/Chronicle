

# Increase Description Text Opacity on My Stories Cards

Change the description text under card titles in `StoryHub.tsx` from 30% to 70% opacity.

**File**: `src/components/chronicle/StoryHub.tsx`

**Change**: Update the description `<p>` tag class from `text-[rgba(248,250,252,0.3)]` to `text-[rgba(248,250,252,0.7)]`.

This is a single line change on the ScenarioCard component's description text (~line 99).

