

# Update Side Characters Header and Remove Empty State Text

## Changes

Two small updates in `src/components/chronicle/ChatInterfaceTab.tsx`:

1. **Line 2713** -- Update the "Side Characters" header to match the "Main Characters" slate blue styling: change `text-slate-500 bg-slate-100` to `text-white bg-[#4a5f7f]`.

2. **Lines 2729-2731** -- Remove the "No side characters yet." empty state text entirely.

