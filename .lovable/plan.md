
# Change Your Stories Page Background to Black

## Change

**File:** `src/pages/Index.tsx`, line 1680

Update the container `div` wrapping the ScenarioHub (Your Stories tab) to have a black background by default. When a custom background image is set, the image will still display over it.

Current class: `"relative w-full h-full"`
Updated class: `"relative w-full h-full bg-black"`
